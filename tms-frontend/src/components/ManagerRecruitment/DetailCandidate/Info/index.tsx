import React from 'react';
import FormInfoCandidate from '../../FormInfoCandidate';
import styles from '@/styles/Recruitment/ManagerRecruitment.module.scss';

const Info = () => {
    return (
        <FormInfoCandidate isViewInfo className={styles.viewInfo} />
    )
}

export default Info;